[build]
  base = "admin-web"
  command = "cd .. && npm ci && npm --prefix admin-web ci && npx prisma generate && npx prisma migrate deploy && npm --prefix admin-web run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
